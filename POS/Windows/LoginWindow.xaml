<Window x:Class="KosovaPOS.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Identifikohu" Height="600" Width="500"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Border Background="White" CornerRadius="6" Padding="40">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.25" BlurRadius="30" ShadowDepth="10" Direction="270"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Logo/Icon -->
            <Border Grid.Row="0" Background="{DynamicResource HeaderGradient}" 
                    CornerRadius="6" Width="100" Height="100"
                    HorizontalAlignment="Center" Margin="0,0,0,20">
                <TextBlock Text="&#x1F510;" FontSize="60" 
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            
            <!-- Title -->
            <TextBlock Grid.Row="1" Text="Mirë se vini" 
                       FontSize="20" FontWeight="SemiBold" 
                       HorizontalAlignment="Center" Margin="0,0,0,10"
                       Foreground="{StaticResource TextPrimary}"/>
            
            <TextBlock Grid.Row="2" Text="Identifikohu për të vazhduar në sistem" 
                       FontSize="14"  
                       HorizontalAlignment="Center" Margin="0,0,0,30"
                       Foreground="{StaticResource TextSecondary}"/>
            
            <!-- Username Field -->
            <StackPanel Grid.Row="3" Margin="0,0,0,20">
                <TextBlock Text="Përdoruesi" FontWeight="SemiBold" FontSize="14"
                           Margin="0,0,0,8" Foreground="{StaticResource TextPrimary}"/>
                <Border Background="{StaticResource SurfaceColor}" 
                        BorderBrush="{StaticResource BorderColor}" 
                        BorderThickness="2" CornerRadius="6" Padding="15,12">
                    <TextBox x:Name="UsernameTextBox" 
                             Background="Transparent" BorderThickness="0"
                             FontSize="14" VerticalContentAlignment="Center"
                             KeyDown="UsernameTextBox_KeyDown"/>
                </Border>
            </StackPanel>
            
            <!-- Password Field -->
            <StackPanel Grid.Row="4" Margin="0,0,0,15">
                <TextBlock Text="Fjalëkalimi" FontWeight="SemiBold" FontSize="14"
                           Margin="0,0,0,8" Foreground="{StaticResource TextPrimary}"/>
                <Border Background="{StaticResource SurfaceColor}" 
                        BorderBrush="{StaticResource BorderColor}" 
                        BorderThickness="2" CornerRadius="6" Padding="15,12">
                    <PasswordBox x:Name="PasswordBox"
                                 Background="Transparent" BorderThickness="0"
                                 FontSize="14" VerticalContentAlignment="Center"
                                 KeyDown="PasswordBox_KeyDown"/>
                </Border>
            </StackPanel>
            
            <!-- Error Message -->
            <Border Grid.Row="5" Background="#FFF3F3F3" CornerRadius="6" Padding="12"
                    x:Name="ErrorBorder" Visibility="Collapsed" Margin="0,0,0,20">
                <TextBlock x:Name="ErrorText" 
                           Text="" Foreground="#F44336" FontSize="14"
                           TextWrapping="Wrap" TextAlignment="Center"/>
            </Border>
            
            <!-- Buttons -->
            <Grid Grid.Row="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" x:Name="LoginButton" Content="&#x1F513; Identifikohu" 
                        Height="44" FontSize="14" FontWeight="SemiBold"
                        Background="{StaticResource PrimaryColor}" Foreground="White"
                        BorderThickness="0" Cursor="Hand"
                        Click="LoginButton_Click" IsDefault="True">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" 
                                    CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <Border.Effect>
                                    <DropShadowEffect Color="#2196F3" Opacity="0.4" BlurRadius="15" ShadowDepth="0"/>
                                </Border.Effect>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDark}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <Button Grid.Column="2" x:Name="CancelButton" Content="Anulo" 
                        Height="44" FontSize="14" FontWeight="SemiBold"
                        Background="{StaticResource SurfaceColor}" 
                        Foreground="{StaticResource TextPrimary}"
                        BorderBrush="{StaticResource BorderColor}" BorderThickness="2"
                        Cursor="Hand"
                        Click="CancelButton_Click" IsCancel="True">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{StaticResource BackgroundColor}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
