<Window x:Class="KosovaPOS.Windows.PurchaseEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Blerje e re" Height="750" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundColor}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="&#x1F4DD; Blerje e re" FontSize="20" FontWeight="SemiBold" 
                   Foreground="{StaticResource PrimaryColor}" Margin="0,0,0,20"/>

        <!-- Form -->
        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <!-- Document Info -->
                <Border Style="{StaticResource ModernCard}" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,10,15">
                            <TextBlock Text="Nr. Dokumentit:" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox x:Name="DocumentNumberTextBox" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Grid.Row="0" Margin="10,0,0,15">
                            <TextBlock Text="Data:" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <DatePicker x:Name="DatePicker" FontSize="14" Height="36"/>
                        </StackPanel>

                        <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,10,15">
                            <TextBlock Text="Furnizuesi: *" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <ComboBox x:Name="SupplierComboBox" Style="{StaticResource ModernComboBox}"
                                      DisplayMemberPath="Name" SelectedValuePath="Id"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Grid.Row="1" Margin="10,0,0,15">
                            <TextBlock Text="Lloji i blerjes:" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <ComboBox x:Name="PurchaseTypeComboBox" Style="{StaticResource ModernComboBox}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2">
                            <CheckBox x:Name="IsPaidCheckBox" Content="Blerja është paguar" FontSize="14"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Items Section -->
                <Border Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <TextBlock Text="Artikujt e blerjes" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>

                        <!-- Add Item -->
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <ComboBox Grid.Column="0" x:Name="ArticleComboBox" 
                                      Style="{StaticResource ModernComboBox}"
                                      Margin="0,0,10,0"
                                      IsEditable="True"
                                      IsTextSearchEnabled="True"
                                      StaysOpenOnEdit="True"
                                      TextSearch.TextPath="Name"
                                      SelectionChanged="ArticleComboBox_SelectionChanged"
                                      PreviewTextInput="ArticleComboBox_PreviewTextInput"
                                      PreviewKeyDown="ArticleComboBox_PreviewKeyDown"
                                      KeyUp="ArticleComboBox_KeyUp">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Barcode}" Width="100" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Name}" Width="200"/>
                                            <TextBlock Text="{Binding PurchasePrice, StringFormat='Blerje: {0:N2}€'}" 
                                                       Foreground="#FF9800" Width="100"/>
                                            <TextBlock Text="{Binding SalesPrice, StringFormat='Shitje: {0:N2}€'}" 
                                                       Foreground="#4CAF50" Width="100"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>

                            <TextBox Grid.Column="1" x:Name="QuantityTextBox" 
                                     Style="{StaticResource ModernTextBox}"
                                     Text="1" Margin="0,0,10,0" ToolTip="Sasia"
                                     PreviewTextInput="NumericTextBox_PreviewTextInput"
                                     GotFocus="NumericTextBox_GotFocus"/>

                            <TextBox Grid.Column="2" x:Name="PurchasePriceTextBox" 
                                     Style="{StaticResource ModernTextBox}"
                                     Text="0.00" Margin="0,0,10,0" ToolTip="Çmimi i blerjes"
                                     PreviewTextInput="NumericTextBox_PreviewTextInput"
                                     GotFocus="NumericTextBox_GotFocus"/>

                            <TextBox Grid.Column="3" x:Name="SalesPriceTextBox" 
                                     Style="{StaticResource ModernTextBox}"
                                     Text="0.00" Margin="0,0,10,0" ToolTip="Çmimi i shitjes"
                                     PreviewTextInput="NumericTextBox_PreviewTextInput"
                                     GotFocus="NumericTextBox_GotFocus"/>

                            <TextBox Grid.Column="4" x:Name="RabatTextBox" 
                                     Style="{StaticResource ModernTextBox}"
                                     Text="0" Margin="0,0,10,0" ToolTip="Rabat %"
                                     PreviewTextInput="NumericTextBox_PreviewTextInput"
                                     GotFocus="NumericTextBox_GotFocus"/>

                            <Button Grid.Column="5" Content="? Shto" 
                                    Style="{StaticResource ModernButton}"
                                    Click="AddItem_Click"/>
                        </Grid>

                        <!-- Column Headers for Add Item -->
                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Artikulli" FontWeight="SemiBold" FontSize="14" Foreground="Gray"/>
                            <TextBlock Grid.Column="1" Text="Sasia" FontWeight="SemiBold" FontSize="14" Foreground="Gray"/>
                            <TextBlock Grid.Column="2" Text="Ç. Blerjes" FontWeight="SemiBold" FontSize="14" Foreground="Gray"/>
                            <TextBlock Grid.Column="3" Text="Ç. Shitjes" FontWeight="SemiBold" FontSize="14" Foreground="Gray"/>
                            <TextBlock Grid.Column="4" Text="Rabat %" FontWeight="SemiBold" FontSize="14" Foreground="Gray"/>
                        </Grid>

                        <!-- Items DataGrid -->
                        <DataGrid x:Name="ItemsDataGrid" 
                                  Style="{StaticResource POSDataGrid}"
                                  AutoGenerateColumns="False"
                                  Height="200">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Artikulli" Binding="{Binding ArticleName}" Width="*"/>
                                <DataGridTextColumn Header="Sasia" Binding="{Binding Quantity, StringFormat=N2, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Width="80"/>
                                <DataGridTextColumn Header="Ç.Blerjes" Binding="{Binding PurchasePrice, StringFormat=N2, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Width="90"/>
                                <DataGridTextColumn Header="Ç.Shitjes" Binding="{Binding SalesPrice, StringFormat=N2, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Width="90"/>
                                <DataGridTextColumn Header="Rabat %" Binding="{Binding RabatPercent, StringFormat=N1, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Width="70"/>
                                <DataGridTextColumn Header="TVSH %" Binding="{Binding VATRate, StringFormat=N0}" Width="60"/>
                                <DataGridTextColumn Header="Totali" Binding="{Binding TotalValue, StringFormat=N2}" Width="90"/>
                                <DataGridTemplateColumn Header="" Width="50">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="&#x1F5D1;" Click="RemoveItem_Click" 
                                                    Background="{StaticResource DangerColor}"
                                                    Foreground="White" BorderThickness="0" Cursor="Hand"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Total -->
                        <Grid Margin="0,15,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="200"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1">
                                <Grid Margin="0,0,0,5">
                                    <TextBlock Text="Totali pa TVSH:" HorizontalAlignment="Left"/>
                                    <TextBlock x:Name="SubtotalText" Text="0.00 €" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                                </Grid>
                                <Grid Margin="0,0,0,5">
                                    <TextBlock Text="TVSH:" HorizontalAlignment="Left"/>
                                    <TextBlock x:Name="VATText" Text="0.00 €" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                                </Grid>
                                <Border Margin="0,5,0,0" Background="{StaticResource PrimaryLight}" Padding="5" CornerRadius="6">
                                    <Grid>
                                        <TextBlock Text="TOTALI:" HorizontalAlignment="Left" FontWeight="SemiBold" FontSize="16"/>
                                        <TextBlock x:Name="TotalText" Text="0.00 €" HorizontalAlignment="Right" 
                                                   FontWeight="SemiBold" FontSize="16" Foreground="{StaticResource PrimaryDark}"/>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Content="&#x1F4BE; Ruaj" Style="{StaticResource ModernButton}"
                    Click="Save_Click" Width="120" Height="44" Margin="0,0,10,0"
                    Background="{StaticResource SuccessColor}"/>

            <Button Grid.Column="2" Content="✗ Anulo" Style="{StaticResource ModernButton}"
                    Click="Cancel_Click" Width="120" Height="44" 
                    Background="#475569"/>
        </Grid>
    </Grid>
</Window>
