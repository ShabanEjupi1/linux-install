<Window x:Class="KosovaPOS.Windows.CategoryEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Shto/Modifiko Kategori"
        Height="480" Width="560"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">

    <Window.Resources>
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>

        <Style x:Key="FormTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#0284C7"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#94A3B8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.88"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="6" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource HeaderGradient}" Padding="25,20">
                <StackPanel>
                    <TextBlock Text="&#x1F355; Menaxhimi i Kategorive"
                               FontSize="20" FontWeight="SemiBold"
                               Foreground="White"/>
                    <TextBlock Text="Plotëso të dhënat e kategorisë dhe ruaj"
                               FontSize="14" Foreground="White" Opacity="0.9"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Form -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="28,22">
                <StackPanel>
                    <TextBlock Text="Emri i Kategorisë *" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="NameTextBox" Style="{StaticResource FormTextBox}"/>

                    <TextBlock Text="Përshkrimi" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="DescriptionTextBox"
                             Style="{StaticResource FormTextBox}"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             MinLines="3"
                             MaxHeight="100"/>

                    <TextBlock Text="Ikona (Emoji)" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="IconTextBox" Style="{StaticResource FormTextBox}"/>

                    <TextBlock Text="Renditja" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="DisplayOrderTextBox"
                             Style="{StaticResource FormTextBox}"
                             Text="0"/>

                    <CheckBox x:Name="IsActiveCheckBox"
                              Content="Aktiv"
                              IsChecked="True"
                              FontSize="14"
                              Margin="0,4,0,0"
                              Foreground="{DynamicResource TextPrimaryBrush}"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Buttons -->
            <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0"
                    Padding="28,16">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="✗ Anulo" Click="CancelButton_Click"
                            Style="{StaticResource ActionButton}"
                            Background="#64748B"
                            Width="120" Margin="0,0,12,0"
                            Padding="20,0"/>
                    <Button Content="&#x1F4BE; Ruaj" Click="SaveButton_Click"
                            Style="{StaticResource ActionButton}"
                            Background="#22C55E"
                            Width="130"
                            Padding="20,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
